name: Jellyfin Plugin Release

on:
  release:
    types:
      - created
      - edited
      - prereleased
      - released

permissions:
  contents: write
  deployments: write
  id-token: write
  pages: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Release Package
    runs-on: ubuntu-latest
    outputs:
      abi-version: ${{ steps.abi-version.outputs.version }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Patch version
        run: sed -i "/Version/s/>.*</>${{ github.ref_name }}.0</" Directory.Build.props
      - name: Setup .NET 9.x
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9
      - name: Restore NuGet Packages
        run: dotnet restore
      - name: .NET Publish
        run: dotnet publish --configuration Release --output publish Jellyfin.Plugin.Bangumi
      - name: Resolve Target ABI Version
        id: abi-version
        shell: pwsh
        run: |
          $dll = Get-ChildItem -Path ~/.nuget/packages/jellyfin.controller -Filter "MediaBrowser.Controller.dll" -Recurse | Select-Object -First 1
          $version = [System.Reflection.AssemblyName]::GetAssemblyName($dll.FullName).Version.ToString()
          echo "version=$version" >> $env:GITHUB_OUTPUT
      - name: Generate Jellyfin Plugin Metadata
        shell: pwsh
        env:
          CHANGELOG: ${{ github.event.release.body }}
        run: |
          @{
              category = "Metadata"
              changelog = $env:CHANGELOG
              description = "Jellyfin bgm.tv 数据源插件，用于拉取中文番剧信息及图片。"
              guid = "1d88c399-c0bb-4cad-81a8-1df8783b79ba"
              name = "Bangumi"
              overview = "bgm.tv metadata provider for Jellyfin"
              owner = "Astatine-213-Tian"
              targetAbi = "${{ steps.abi-version.outputs.version }}"
              timestamp = Get-Date -AsUTC -Format s
              version = "${{ github.ref_name }}.0"
              imageUrl = "https://astatine-213-tian.github.io/jellyfin-plugin-bangumi/poster.png"
          } | ConvertTo-Json -Compress | Out-File "publish/meta.json" -Encoding UTF8 -NoNewline
      - name: Create Release Artifact
        run: zip -9j Jellyfin.Plugin.Bangumi.zip publish/*.dll publish/meta.json
      - uses: actions/upload-artifact@v4
        with:
          name: Jellyfin.Plugin.Bangumi
          path: Jellyfin.Plugin.Bangumi.zip
      - name: .NET Publish (Emby)
        run: dotnet publish --configuration Release --output publish Emby.Plugin.Bangumi
      - uses: actions/upload-artifact@v4
        with:
          name: Emby.Plugin.Bangumi
          path: publish/Emby.Plugin.Bangumi.dll
  upload:
    name: Upload to GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: Jellyfin.Plugin.Bangumi
      - uses: actions/download-artifact@v4
        with:
          name: Emby.Plugin.Bangumi
      - name: Upload to Release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: "${{ github.token }}"
          tag: ${{ github.ref_name }}
          overwrite: true
          file_glob: true
          file: "*.Plugin.Bangumi.{dll,zip}"
  repository:
    name: Update Plugin Repository
    runs-on: ubuntu-latest
    needs: [build, upload]
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: Jellyfin.Plugin.Bangumi
      - name: Create Plugin Repository for Jellyfin
        shell: pwsh
        env:
          CHANGELOG: ${{ github.event.release.body }}
          TOKEN: ${{ github.token }}
          VERSION: ${{ github.ref_name }}.0
          ABI_VERSION: ${{ needs.build.outputs.abi-version }}
        run: |
          New-Item -ItemType Directory -Force -Path Release
          $PagesUrl = "https://astatine-213-tian.github.io/jellyfin-plugin-bangumi"
          try {
            $Repository = Invoke-WebRequest "$PagesUrl/repository.json" | % Content | ConvertFrom-Json
            $Repository[0].versions = $Repository[0].versions | Where-Object { $_.version -ne $env:VERSION }
            foreach ($Version in $Repository[0].versions) {
              if ([string]::IsNullOrEmpty($Version.targetAbi)) {
                $Version.targetAbi = "10.10.0.0"
              }
              Invoke-WebRequest -Uri $Version.sourceUrl -OutFile "Release/$($Version.version).zip"
              $Version.sourceUrl = "$PagesUrl/$($Version.version).zip"
            }
            $ExistingVersions = $Repository[0].versions
          } catch {
            $ExistingVersions = @()
          }
          Copy-Item -Path Jellyfin.Plugin.Bangumi.zip -Destination Release/$($env:VERSION).zip -Recurse
          Invoke-WebRequest -Uri https://raw.githubusercontent.com/Astatine-213-Tian/jellyfin-plugin-bangumi/refs/heads/master/poster.png -OutFile Release/poster.png
          ConvertTo-Json -Compress -Depth 5 @(@{
            category = "Metadata"
            description = "bgm.tv metadata provider for Jellyfin"
            guid = "1d88c399-c0bb-4cad-81a8-1df8783b79ba"
            name = "Bangumi"
            overview = "Jellyfin bgm.tv 数据源插件，用于拉取中文番剧信息及图片。"
            imageUrl = "$PagesUrl/poster.png"
            owner = "Astatine-213-Tian"
            versions = @(@{
              checksum =  (Get-FileHash -Algorithm MD5 "Jellyfin.Plugin.Bangumi.zip").Hash.ToLower()
              changelog = $env:CHANGELOG
              targetAbi = $env:ABI_VERSION
              sourceUrl = "$PagesUrl/$($env:VERSION).zip"
              timestamp = Get-Date -AsUTC -Format s
              version = $env:VERSION
              imageUrl = "$PagesUrl/poster.png"
            }) + $ExistingVersions
          }) | Out-File Release/repository.json -Encoding UTF8NoBOM -NoNewline
      - name: Generate GitHub Pages Artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-repository
          path: Release/*
          retention-days: 1
  deploy-github-pages:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: repository
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: plugin-repository
          path: pages
      - name: Repack Artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages/
      - name: Deploy
        uses: actions/deploy-pages@v4
